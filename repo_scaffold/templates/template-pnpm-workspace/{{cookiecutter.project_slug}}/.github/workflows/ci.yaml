name: CI

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

jobs:
  ci:
    uses: ShawnDen-coder/repo-scaffold/.github/workflows/reusable-node-ci.yaml@0.27.0
    with:
      lint-command: pnpm format:check
      build-command: pnpm -r run build

