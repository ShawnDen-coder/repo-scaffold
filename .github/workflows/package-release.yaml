name: 🚀 Release Build & Publish
on:
  push:
    tags:
      - '[0-9]+.[0-9]+.[0-9]+'
permissions:
  contents: write
  id-token: write
jobs:
  release:
    uses: ./.github/workflows/reusable-python-release.yaml
    with:
      tag: ${{ github.ref_name }}
      workspace: false
      deploy-docs: false
    secrets: inherit
